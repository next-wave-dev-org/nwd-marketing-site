---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---
<div class:list={["mx-auto flex h-[280px] w-[280px] shrink-0 items-center justify-center rounded-full bg-accent-primary border border-border-subtle p-6 text-center md:mx-0 md:h-[380px] md:w-[380px] md:p-8 shadow-2xl", className]}>
    <slot />
</div>