---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import ContactSection from '../../components/ContactSection.astro';
import GraduatesSection from '../../components/GraduatesSection.astro';
import StaffGrid from '../../components/StaffGrid.astro';

// 11 total leadership members to feed the 4-3-4 row component
const leadershipMembers = Array(11).fill({ name: 'Bob Smith', role: 'Program Manager' });

// Safely fetch the collection so the page doesn't crash if it's empty
let teamNames = ['Bob Smith', 'Bob Smith', 'Bob Smith', 'Bob Smith'];
try {
  const allGrads = await getCollection('graduates');
  if (allGrads && allGrads.length > 0) {
    teamNames = allGrads.map(g => g.data.name || 'Bob Smith');
  }
} catch (e) {
  console.warn("Graduates collection not found, using fallback data.");
}
---

<Layout title="Our Org | Next Wave Dev" description="Building our community and impact">
  <div class="mx-auto w-full max-w-[1440px] overflow-hidden bg-light-aqua px-5 pb-16 pt-10 text-black md:px-8 md:pb-24 md:pt-12">
    
    <!-- Hero Section -->
    <section class="mx-auto w-full max-w-[1202px]">
      <div class="grid grid-cols-[186px_1fr] items-center gap-6 md:grid-cols-[529px_1fr] md:gap-[128px]">
        
        <!-- Badge -->
        <div class="relative h-[220px] w-[186px] overflow-hidden rounded-[10px] bg-gradient-to-b from-light-aqua to-secondary-blue md:h-[560px] md:w-full">
          <div class="absolute left-1/2 top-1/2 h-[160px] w-[160px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-primary-blue md:h-[420px] md:w-[420px]"></div>
          <h1 class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 text-center font-heading text-[32px] font-bold leading-[1.15] text-hero-text drop-shadow-[0_4px_4px_rgba(0,0,0,0.25)] md:text-[72px]">
            Our<br/>Org
          </h1>
        </div>
        
        <!-- Text Content -->
        <div class="max-w-[600px] pl-4 pr-1 md:max-w-[440px] md:translate-x-[130px] md:pl-0 md:pr-0">
          <h2 class="font-roboto text-[28px] font-bold leading-tight text-black md:text-[56px] md:leading-[64px]">
            Building our community and impact
          </h2>
          <p class="mt-3 font-sans text-[16px] font-normal leading-relaxed text-black md:mt-4 md:text-[24px] md:leading-[33px]">
            Placeholder text here to fill this section to make it look real. This body text is just random fill to fill this section to make things look real for placeholder purposes. Also more words.
          </p>
        </div>
        
      </div>
    </section>

    <!-- Leadership Grid (4-3-4 Component) -->
    <section class="mx-auto mt-24 flex w-full max-w-[1328px] flex-col items-center md:mt-[180px]">
      <h2 class="mb-12 text-center font-roboto text-[40px] font-bold leading-tight text-black md:mb-[80px] md:text-[48px]">
        Leadership
      </h2>
      <StaffGrid staff={leadershipMembers} />
    </section>

    <!-- Team Section (Passes "Team" as the title so it overrides "Graduates") -->
    <section class="w-full mt-8 md:mt-12">
      <GraduatesSection graduates={teamNames} title="Team" />
    </section>

    <!-- Contact Form -->
    <section class="mx-auto mt-20 w-full max-w-[1125px] relative z-10 md:mt-24">
       <ContactSection />
    </section>

  </div>
</Layout>